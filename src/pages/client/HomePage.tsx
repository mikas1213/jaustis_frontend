import { HeroSection, StatisticsSection } from '../../features/client/home/components';

const HomePage = () => {
	return (
        <>
            <HeroSection />
            <StatisticsSection />
        </>
	);
};

export default HomePage;
